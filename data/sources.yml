sources:
  douglas:
    path: data/raw/parquet/douglas.parquet
    format: parquet
    columns:
      - series_code
      - year
      - value

  uscb:
    path: data/raw/parquet/uscb.parquet
    format: parquet
    columns:
      - series_code
      - year
      - value
