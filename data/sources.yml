sources:
  douglas:
    path: data/raw/json_gz/douglas.zip
    format: zip_json
    data_file: data.json.gz
    columns:
      - series_id
      - period
      - value

  uscb:
    path: data/raw/json_gz/uscb.zip
    format: zip_json
    data_file: data.json.gz
    columns:
      - series_id
      - period
      - value
